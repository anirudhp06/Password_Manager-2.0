#just ignore the code written here, this is like an archive
#I use this to copy and paste other lines of codes which i have implemented.
#But dont use very often.

"""
username=input('Enter username:')
password=getpass('Enter the password:')
"""

#query='insert into testing values("prateek")'
#cursor_obj.execute(query)

#for i in output:
#    print(i)
#cursor_obj.executemany(query, val)

#secs=int(input("Enter roll no to get the name:"))
"""cursor_obj.execute('select name from testing;')
names=[]
op=cursor_obj.fetchall()
cursor_obj.execute('select rollno from testing;')
rolss=[]
ops=cursor_obj.fetchall()"""
"""if op==[]:
    print('No records found')
else:
    for i in op:
        for j in i:
           #cls
           #  print(j,end=" ")
            names.append(j)
        #print()
    for i in ops:
        for j in i:
            #print(j,end=" ")
            rolss.append(j)
        #print()
for i in range(len(names)):
    print(names[i],":",rolss[i])"""

#print('Successfully Logged out after committing')

